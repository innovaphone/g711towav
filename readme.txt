You can then drag and drop a selection of G711a files onto the batch file (or onto a `short-cut' to it). The wav files end up in a folder called 'wav'.

Also from command line the batch file can be used:

C:temp> convert_G711A.bat input_file.g711a
